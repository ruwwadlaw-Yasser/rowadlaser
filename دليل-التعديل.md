# دليل موقع رواد الليزر (للتعديل السريع)

موقع تعريفي احترافي لشركة رواد الليزر — عربي/إنجليزي، سريع التحميل، ومناسب للجوال والكمبيوتر.
هدفه الأساسي **جلب العملاء** عبر أزرار "اطلب عرض سعر" و"واتساب" ونموذج التواصل.

---

## 1) أهم خطوة: ضع بيانات التواصل الحقيقية

افتح الملف: `assets/js/main.js` وعدّل أول جزء فقط (بالأعلى):

```js
window.RUWWAD = {
  whatsapp: '966500000000',            // رقم الواتساب بصيغة دولية بدون علامة +
  phoneDisplay: '+966 50 000 0000',    // الرقم كما يظهر للزائر
  email: 'info@rowadlaser.com',       // بريدكم الرسمي
  city: { ar: 'جدة، المملكة العربية السعودية', en: 'Jeddah, Saudi Arabia' }
};
```

- **whatsapp**: مثال إن كان رقمك `05XXXXXXXX` تكتبه `9665XXXXXXXX`.
- بعد التعديل، تعمل كل أزرار الواتساب والاتصال والبريد ونموذج "اطلب عرض سعر" تلقائياً.

## 2) روابط حسابات التواصل الاجتماعي

في تذييل كل صفحة (أسفل الصفحات) استبدل `href="#"` في أيقونات
إنستغرام / X / لينكدإن بروابط حساباتكم الحقيقية.

## 3) استبدال الصور بمشاريعكم الحقيقية

- **معرض الأعمال** في الصفحة الرئيسية (`index.html`) حالياً بطاقات ملوّنة مؤقتة.
  استبدلها بصور مشاريعكم لأفضل تأثير على العملاء (ابحث عن تعليق «ملاحظة للعميل» داخل الملف).
- **آراء العملاء**: استبدل الأسماء والنصوص بشهادات عملاء حقيقيين.
- **المدونة**: المقالات نموذجية — أضف مقالاتكم بنفس التنسيق.

## 4) الشعارات والخطوط (جاهزة)

- الشعار بتأثير الستانلس ستيل معروض في الصفحة الرئيسية على خلفية بيضاء.
- الخطوط: تم استخدام **خط ثمانية** للعبارات الرئيسية (مضمّن داخل الموقع لسرعة التحميل).
- جميع ملفات الهوية في مجلد `assets/logos/`.

## 5) كيف أعرض الموقع أو أنشره؟

- **للمعاينة على جهازك**: افتح ملف `index.html` مباشرة بالمتصفح.
- **للنشر على الإنترنت**: ارفع مجلد `موقع` بالكامل على أي استضافة
  (مثل: Netlify, Vercel, GitHub Pages, أو استضافة عادية). لا يحتاج الموقع لأي خادم خاص.
- ربط اسم نطاق (دومين) مثل `rowadlaser.com` يتم من لوحة الاستضافة.

## بنية الملفات

```
موقع/
├── index.html        الصفحة الرئيسية (Landing)
├── services.html     الخدمات
├── about.html        من نحن
├── blog.html         المدونة
├── post.html         نموذج مقال
├── contact.html      تواصل معنا + نموذج طلب عرض سعر
├── دليل-التعديل.md    هذا الملف
└── assets/
    ├── css/styles.css   التصميم
    ├── js/main.js       الإعدادات + التفاعل (عدّل بيانات التواصل هنا)
    ├── fonts/           خط ثمانية (woff2)
    ├── logos/           الشعارات
    └── img/             الأيقونة المفضلة (favicon)
```

> لتغيير اللغة يضغط الزائر زر **ع / EN** أعلى الصفحة، ويُحفظ اختياره تلقائياً.
